export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  BLOCKED = 'blocked',
  ARCHIVED = 'archived',
}

export enum Role {
  ADMIN = 'admin',
  USER = 'user',
  MANAGER = 'manager',
}

export enum ProductStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
}

export enum Size {
  SMALL = 'sm',
  MEDIUM = 'md',
  LARGE = 'lg',
}
