export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  BLOCKED = 'blocked',
  ARCHIVED = 'archived',
}

export enum Role {
  ADMIN = 'admin',
  USER = 'user',
  MANAGER = 'manager',
}
